name: CI/CD Docker Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Prepare Docker env
        run: cp .env.docker.example .env

      - name: Build Docker image
        run: docker compose build

  deploy:
    name: Deploy to DigitalOcean
    runs-on: ubuntu-latest
    needs: validate

    steps:
      - name: Check deployment secrets
        env:
          DO_HOST: ${{ secrets.DO_HOST }}
          DO_USER: ${{ secrets.DO_USER }}
          DO_SSH_KEY: ${{ secrets.DO_SSH_KEY }}
          APP_HEALTH_URL: ${{ secrets.APP_HEALTH_URL }}
        run: |
          test -n "$DO_HOST" || (echo "Missing secret: DO_HOST" && exit 1)
          test -n "$DO_USER" || (echo "Missing secret: DO_USER" && exit 1)
          test -n "$DO_SSH_KEY" || (echo "Missing secret: DO_SSH_KEY" && exit 1)
          test -n "$APP_HEALTH_URL" || (echo "Missing secret: APP_HEALTH_URL" && exit 1)

      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USER }}
          key: ${{ secrets.DO_SSH_KEY }}
          script: |
            set -euo pipefail
            cd /opt/mini-tienda-api
            git fetch origin main
            git reset --hard origin/main
            docker compose up -d --build
            docker image prune -f

      - name: Verify production health
        run: |
          for attempt in {1..12}; do
            if curl -fsS "${{ secrets.APP_HEALTH_URL }}"; then
              exit 0
            fi
            sleep 5
          done
          echo "Health check failed after deployment"
          exit 1
